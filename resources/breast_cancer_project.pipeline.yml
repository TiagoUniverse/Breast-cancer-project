# The main pipeline for breast_cancer_project
resources:
  pipelines:
    breast_cancer_project_pipeline:
      name: breast_cancer_project_pipeline
      catalog: workspace
      schema: breast_cancer_project_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
